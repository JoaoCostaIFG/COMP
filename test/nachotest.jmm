        /* asd adsd asp dkapsk d
        apdkaspokdopask
        asdioajdoiaj*/
import amazing.lambreta;
import segundo.importa.com.maior.path;

class Nachos extends Cute {
    int field1;
    bool field2;
    int[] field3;

	public int /* a */ metodo() {
	    int EOF;

	    EOF = 0xa8192;
        boots[0] = 10l + 9l * 8l;
        boots[0L] = 7l * 6l + 5l;

        numero = 129_2312_23_12l;  // not integer (too >)

        1<2[1][0];
        this.a(1,2)[1][0];

        new int[1 < 2][this.a(1,2)[1][0]];
        this.a(new int[1])[new int[2]];

        L[i] = L.length - i;
        !false.length; // TODO :)
        !new int[nachos[2]].length < true;
        true.break(1.length);
        false.$(9999l - 10000L) < 1;
        false[true] < $$$$$$$$$$$$$$$$$;
        a.length.length.length;

        true && this && false;
        this.asd(this.asd(true), false);

	    while (asj$d7as_211d$sa < sim) {
            if (boots < cats) {
                boots = cats;
            }
            else {
                true;
            }
	    }

	    return 2;
	}
}
