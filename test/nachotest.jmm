        /* asd adsd asp dkapsk d
        apdkaspokdopask
        asdioajdoiaj*/
import amazing.lambreta;
import segundo.importa.com.maior.path;

class Nachos extends Cute {
    int field1;
    bool field2;
    int[] field3;

    public static void main(String[] a) {

    }

    /*
	public int metodo(int param1, ABC param2) {
	    return 0;
	}
	*/

	public int metodo(int param1, ABC param2) {
	    int EOF;
	    int KKKKKK;
	    int numero;
	    int[] boots;
	    int asj$d7as_211d$sa;
	    int sim;
	    int cats;

        cats = 99999999999;
	    boots = new int[2];
	    EOF = 0xa8192;
        boots[0] = 10l + 9l * 8l;
        boots[0L] = 7l * 6l + 5l;

        numero = 129_2312_23_12l;  // not integer (too >)

        this.a(1,2)[1];

        new int[0][1]; // TODO

        1 < 2;
        // !false < true;
        // true.break(1.length);
        // false.$(9999l - 10000L) < false;
        // false[true] < $$$$$$$$$$$$$$$$$;
        // a.length.length.length;

        true && true && false;
        this.asd(this.asd(true), false);

	    asj$d7as_211d$sa = 1;
	    sim = 2;
	    while (asj$d7as_211d$sa < sim) {
            if (boots[0] < cats) {
                boots[2] = cats;
            }
            else {
                true;
            }
	    }

	    return 2;
	}
}
